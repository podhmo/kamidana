0.11.0

- add --data-json option: pass a literal JSON object as template data; -d/--data and --data-json merge in argv order, -i stdin still wins (#86)
- add --strict-undefined option (#31); default to StrictUndefined, --no-strict-undefined opts out (#85)
- load templates from python packages (#34); document '../' physical-path prefix and the template name spec
- gentle error: rework traceback extraction for jinja2 3.x frame names (Environment optimized=False); 'where:' points at the actual raise site, skips stdlib/tool-internal frames, and internal frames stay in the Traceback body (#60, #63, #64, #65, #72, #87)
- gentle error: clearer message when a -a/--additionals module is missing (#62); better not-found template errors
- replace magicalimport with the in-repo importer kamidana._import (relative imports inside packages); drop fastentrypoints and importlib_resources
- update dependencies: jinja2>=3.1, dictknife[load]>=0.14, inflection>=0.5
- drop python <3.10 support (3.9 is EOL; CI covers 3.10-3.14)
- packaging: move to src layout and pyproject.toml, README.md in markdown, ship py.typed
- typing: typing.Protocol interfaces, full annotations, mypy --strict enforced in CI
- run examples regression (make ci) on CI
- remove Sphinx/ReadTheDocs scaffolding (#91)

0.10.0

- follow jinja2 3.x API changes (#51)
- yaml/toml loading via dictknife[load] extras (ruamel.yaml, tomlkit)
- add LICENSE file (MIT) (#48)
- set up GitHub Actions CI (#50)

0.9.1

- catch up magicalimport 0.8.1

0.9.0

- python 3.8 support (#43)

0.8.2

- more aggressive support for cookiecutter, enable to use relative phisical path

0.8.1

- refine kamidana.additionals.naming implementation

0.8.0

- add creating jinja2's extension mini-framework, and cookiecutter support
- add kamidana.additionals.env
- add `--list-info` option

0.7.2

- on error message, skipping almost duplicated jinja2 frame's output

0.7.1

- update readme.rst

0.7.0

- gentle error message
- changing template path lookup mechanism
- adding newline at end of file
- drop trim block=True

0.6.2

- template is not passed, act as --dump-context (#21)
- same behaviour `-a kamidana.additonals.reader` and `-a reader` (shortcuts) (#18)
- update readme.rst

0.6.1

- kamidana, fix: --dst option is ignored
- kamidana-batch, fix: kamidana-batch ignore --logging option

0.6.0

- kamidana.additionals.naming

0.5.1

- fix read_from_command with a relative path; adjust logging messages

0.5.0

- new kamidana-batch command
- changing addtionals.reader's current working directory path

0.4.1

- fix bug (multi -a option)

0.4.0

- add kamidana.additionals.reader

0.3.0

- add `--extension` option
- add `--dump-context` option

0.2.1

- rename Loader to TemplateLoader

0.2.0

- --loader option is added
- interfaces are defined

0.1.1

- fix install requires

0.1.0

- first release
